server:
  port: 8081

spring:
  application:
    name: deposit
  datasource:
    url: jdbc:mysql://localhost:3306/Deposit
    username: 'root'
    password: ''
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
        format_sql: true
    show-sql: true

